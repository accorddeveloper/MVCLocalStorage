@model LocalStorage.Models.Step3

@{
    ViewBag.Title = "Step 3";
}

<h2>Step 3</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Step 3</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Gender)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Gender)
            @Html.ValidationMessageFor(model => model.Gender)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.AnotherField)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.AnotherField)
            @Html.ValidationMessageFor(model => model.AnotherField)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.AndAnother)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.AndAnother)
            @Html.ValidationMessageFor(model => model.AndAnother)
        </div>

        <p>
            <input type="submit" value="Submit" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Previous", "Step2")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
